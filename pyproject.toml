[project]
name = "hreflangcrawler"
version = "0.1.0"
description = "Simple Python script that crawls a website and extracts hreflang tags"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "beautifulsoup4>=4.13.4",
    "pandas>=2.2.3",
    "requests>=2.32.3",
]
